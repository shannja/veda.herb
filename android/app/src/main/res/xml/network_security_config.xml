<?xml version="1.0" encoding="utf-8"?>
<!-- 
  Network Security Configuration for VedaHerb
  Ensures all network traffic is encrypted and secure
-->
<network-security-config>
    <!-- Base configuration: enforce HTTPS for all domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificates only -->
            <certificates src="system" />
            <!-- Optionally trust user certificates for development -->
            <!-- <certificates src="user" /> -->
        </trust-anchors>
    </base-config>
    
    <!-- Domain-specific configurations can be added here if needed -->
    <!-- Example:
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.example.com</domain>
        <pin-set expiration="2025-01-01">
            <pin digest="SHA-256">BASE64_PIN_VALUE</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>